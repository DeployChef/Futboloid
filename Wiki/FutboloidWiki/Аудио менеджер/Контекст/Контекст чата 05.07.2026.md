# Контекст чата — Аудио Система (05.07.2026)

> [!date] Дата сессии
> 5 июля 2026

---

## Что было сделано в этой сессии

### 1. Добавлен свисток при начале матча
- В `BallMotion.cs` метод `Serve()` теперь вызывает `AudioManager.Instance.PlayEvent("MatchStart")`
- Свисток воспроизводится при каждой подаче мяча
- Динамик: `Speaker_Start` с Event Name = `MatchStart`

### 2. Добавлена пауза музыки при выходе в главное меню
- В `AudioManager.cs` добавлены методы:
  - `PauseMusic()` — ставит музыку на паузу (сохраняет позицию)
  - `ResumeMusic()` — возобновляет музыку с того же места
- В `AudioManager.cs` добавлен поиск `_musicSource` по eventName = "MusicStart"
- В `OverlayStateController.cs` добавлена логика:
  - При `OnField → MainMenu`: `AudioManager.Instance.PauseMusic()`
  - При `MainMenu → OnField` (возврат): `AudioManager.Instance.ResumeMusic()`

---

## Файлы, которые были изменены

### Изменённые файлы

1. **`Assets/_Projects/Code/Futboloid.Gameplay/Ball/BallMotion.cs`**
   - В `Serve()`: добавлен `AudioManager.Instance.PlayEvent("MatchStart")`

2. **`Assets/_Projects/Code/Futboloid.Core/AudioManager.cs`**
   - Добавлено: `_musicSource` (поиск динамика музыки)
   - Добавлено: `PauseMusic()` — ставит музыку на паузу
   - Добавлено: `ResumeMusic()` — возобновляет музыку

3. **`Assets/_Projects/Code/Futboloid.Main/Navigation/OverlayStateController.cs`**
   - В `SetState()`: добавлена логика паузы/возобновления музыки при переключении состояний

---

## Архитектура аудио-системы (обновлённая)

### Компоненты

```
AudioManager (синглтон)
├── Speaker_BallHit (AudioClipSource)
│   ├── eventName: "BallHit"
│   ├── clips: [удар1.wav, удар2.wav, удар3.wav]
│   ├── priority: 80
│   ├── cooldown: 0.3
│   ├── loop: false
│   ├── enableFade: false
│   └── mixerGroup: SFX
├── Speaker_Goal (AudioClipSource)
│   ├── eventName: "GoalScored"
│   ├── priority: 0
│   └── ...
├── Speaker_Music (AudioClipSource)
│   ├── eventName: "MusicStart"
│   ├── loop: true
│   ├── enableFade: true
│   ├── fadeDuration: 1.5
│   └── mixerGroup: Music
├── Speaker_Start (AudioClipSource)
│   ├── eventName: "MatchStart" (свисток)
│   └── ...
└── ...
```

### Логика работы

```
Игровой процесс
  ↓
Событие (удар, гол, подача)
  ↓
Код вызывает: AudioManager.Instance.PlayEvent("Имя")
  ↓
AudioManager ищет динамик по имени
  ↓
Проверка:
  1. Allow Overlap — можно ли играть?
  2. Cooldown — не слишком ли часто?
  3. Priority — достаточно ли важен?
  4. Max Voices — есть ли место?
  ↓
Если всё ОК:
  1. PlayRandom() — выбирает случайный звук
  2. FadeIn() — плавно включает (если включено)
  3. source.Play() — воспроизводит
  ↓
Звук попадает в AudioMixer (SFX или Music)

---

Выход в главное меню:
  ↓
OverlayStateController: OnField → MainMenu
  ↓
AudioManager.PauseMusic() — музыка ставится на паузу (позиция сохраняется)
  ↓
Возврат в игру:
  ↓
OverlayStateController: MainMenu → OnField
  ↓
AudioManager.ResumeMusic() — музыка возобновляется с того же места
```

### Вызовы из кода

```csharp
// В BallMotion.cs — при ударе мяча
AudioManager.Instance.PlayEvent("BallHit");

// В BallMotion.cs — при голе
AudioManager.Instance.PlayEvent("GoalScored");

// В BallMotion.cs — при подаче мяча (свисток)
AudioManager.Instance.PlayEvent("MatchStart");

// В MatchFlow.cs — при подаче мяча (старт таймера)
AudioManager.Instance.PlayEvent("MusicStart");

// В MatchFlow.cs — при рестарте матча
AudioManager.Instance.StopEvent("MusicStart");

// В MatchFlow.cs — при конце матча
AudioManager.Instance.PlayEvent("MatchEnd");
AudioManager.Instance.StopEvent("MusicStart");

// В OverlayStateController.cs — при выходе в меню
AudioManager.Instance.PauseMusic();

// В OverlayStateController.cs — при возврате в игру
AudioManager.Instance.ResumeMusic();
```

---

## Ключевые решения

### Почему свисток вызывается из BallMotion.Serve()?
- Согласно документации, `BallServedEvent` маппится на `MatchStart`
- Свисток должен звучать при каждой подаче мяча
- `Serve()` — это точка, где мяч становится активным

### Почему пауза музыки через Pause()/Resume(), а не Stop()/Play()?
- `Pause()` и `Resume()` сохраняют позицию воспроизведения
- `Stop()` + `Play()` начинали бы трек с начала
- Игрок ожидает, что музыка продолжится с того же места

### Почему логика паузы в OverlayStateController?
- `OverlayStateController` управляет переключением состояний навигации
- Он знает, когда игрок выходит в меню и когда возвращается
- `AudioManager` не должен знать о навигации — он просто предоставляет методы

---

## История изменений аудио-системы

> **05.07.2026** — Свисток и пауза музыки
> - Добавлен свисток при начале матча в BallMotion.Serve()
> - Добавлена пауза музыки при выходе в главное меню
> - Добавлено возобновление музыки при возврате в игру
> - Методы PauseMusic()/ResumeMusic() в AudioManager

> **04.07.2026** — Fade In/Out и управление музыкой
> - Добавлен Fade In/Out для всех динамиков
> - Добавлен глобальный тумблер EnableFade
> - Добавлен метод StopEvent()
> - Музыка останавливается при Reset() и EndMatch()
> - Добавлено поле loop для цикличного воспроизведения
> - Создана полная инструкция для новичков

> **03.07.2026** — Полное упрощение
> - Убран DI, шина событий, LifetimeScope
> - AudioManager стал синглтоном
> - Удалён BallSounds.cs
> - Прямые вызовы из игровых скриптов

> **02.07.2026** — Приоритетная система
> - Добавлена система приоритетов (0-100)
> - Добавлен контроль наложения (Allow Overlap)
> - Заменена логика CheckVoiceLimit на CanPlayWithPriority

> **30.06.2026** — Полная переделка
> - Система динамиков (AudioClipSource)
> - Исправлены NullReferenceException
> - AudioManager слушает шину через DI
