# Контекст чата — Аудио Менеджер (30.06.2026)

> [!date] Дата
> 30 июня 2026

---

## Проблема

Звуки не воспроизводились при событиях. `AudioManager` не был интегрирован с шиной событий.

### Корневые причины

1. **`AudioManager` не подписан на `IGameEventBus`** — висел на сцене как обычный MonoBehaviour, никто не вызывал `PlayEventSound()`.
2. **`AudioManager` не зарегистрирован в DI** — VContainer не знал о нём, `[Inject]` не сработал.
3. **Событие `BallHit` не публиковалось** — в `BallMotion.ResolveHit()` не было `_bus.Publish(new BallHitEvent())`.
4. **`NullReferenceException` в `Update()`** — `activeSources` мог быть null при первом вызове.

---

## Хронология решений

### Шаг 1: Первая попытка — встроить подписку в AudioManager

**Что сделали:**
- Добавили `[Inject] Construct(IGameEventBus bus)` в `AudioManager`.
- Добавили `OnEnable()` с подпиской на события.
- Зарегистрировали `AudioManager` в `RootLifetimeScope`.

**Ошибка:** `RootLifetimeScope` не видит компоненты на других сценах. `AudioManager` на `Game.unity`, `RootLifetimeScope` на `Root.unity`.

**Ошибка компиляции:** `CS0246: The type or namespace name 'Inject' could not be found` — забыли `using VContainer;` в `Futboloid.Core`.

### Шаг 2: Создать отдельный LifetimeScope

**Что сделали:**
- Создали `AudioManagerLifetimeScope` на сцене `Game.unity`.
- Убрали регистрацию из `RootLifetimeScope`.

**Ошибка:** `CS0246: The type or namespace name 'VContainer' could not be found` — `Futboloid.Core` не должен зависеть от VContainer.

### Шаг 3: Убрать VContainer из Core

**Что сделали:**
- Убрали `using VContainer;` из `AudioManager.cs`.
- Заменили `[Inject] Construct(...)` на конструктор `AudioManager(IGameEventBus bus)`.

**Ошибка:** `CS0104: 'Random' is an ambiguous reference between 'UnityEngine.Random' and 'System.Random'`.

**Что сделали:**
- Заменили `Random.Range()` на `UnityEngine.Random.Range()`.

**Ошибка:** `NullReferenceException: Object reference not set to an instance of an object` в `AudioManager.Update()` — `activeSources` мог быть null.

### Шаг 4: Полная переделка архитектуры

**Пользователь сказал:**
> "Давай сделаем так, что Audio Source это будет у нас динамик. Он будет ловить события, то есть ивенты. Ивенты будут приказывать, чтобы звук воспроизвелся на динамик, а динамик пошел на микшер."

**Что сделали:**
- Создали `AudioClipSource.cs` — компонент-динамик с `AudioSource`, массивом звуков и именем события.
- Переделали `AudioManager.cs` — теперь он собирает дочерние объекты `AudioClipSource`, маппит их по имени и управляет ими.
- Убрали `Update()`, пул AudioSource, кулдауны — всё упростилось.

**Новая архитектура:**
```
AudioManager (мозг)
├── Speaker_BallHit (AudioClipSource)
│   ├── AudioSource
│   ├── clips: [bop1.wav, bop2.wav]
│   ├── eventName: "BallHit"
│   └── mixerGroup: SFX
├── Speaker_Goal (AudioClipSource)
│   ├── AudioSource
│   ├── clips: [goal1.ogg, goal2.ogg]
│   ├── eventName: "GoalScored"
│   └── mixerGroup: SFX
└── ...
```

### Шаг 5: Исправить доступ к source

**Ошибка:** `CS0122: 'AudioClipSource.source' is inaccessible due to its protection level`.

**Что сделали:**
- Заменили `private AudioSource source;` на `public AudioSource source;` в `AudioClipSource.cs`.

---

## Итоговый код

### AudioClipSource.cs
**Путь:** `Assets/_Projects/Code/Futboloid.Core/AudioClipSource.cs`

```csharp
public class AudioClipSource : MonoBehaviour
{
    public string eventName;
    public AudioClip[] clips;
    public AudioMixerGroup mixerGroup;
    public AudioSource source; // Публичный для доступа из AudioManager

    public void PlayRandom()
    {
        if (source == null || clips == null || clips.Length == 0) return;
        if (mixerGroup != null) source.outputAudioMixerGroup = mixerGroup;
        int index = Random.Range(0, clips.Length);
        source.clip = clips[index];
        source.Play();
    }

    public void Stop() => source?.Stop();
}
```

### AudioManager.cs
**Путь:** `Assets/_Projects/Code/Futboloid.Core/AudioManager.cs`

```csharp
public class AudioManager : MonoBehaviour
{
    [SerializeField] private int maxVoices = 8;
    private List<AudioClipSource> sources;
    private Dictionary<string, AudioClipSource> eventMap;
    private readonly IGameEventBus _bus;

    public AudioManager(IGameEventBus bus) { _bus = bus; }

    private void Awake()
    {
        var allSources = GetComponentsInChildren<AudioClipSource>();
        foreach (var source in allSources)
        {
            sources.Add(source);
            if (!string.IsNullOrEmpty(source.eventName))
                eventMap[source.eventName] = source;
        }
        DontDestroyOnLoad(gameObject);
    }

    private void OnEnable()
    {
        if (_bus == null) return;
        _bus.Subscribe<GoalScoredEvent>(e => PlayEvent("GoalScored"));
        _bus.Subscribe<BallReturnedToKeeperEvent>(e => PlayEvent("BallHit"));
        _bus.Subscribe<BallHitEvent>(e => PlayEvent("BallHit"));
        _bus.Subscribe<BallServedEvent>(e => PlayEvent("MatchStart"));
        _bus.Subscribe<MatchEndedEvent>(e => PlayEvent("MatchEnd"));
    }

    private void PlayEvent(string eventName)
    {
        if (!eventMap.TryGetValue(eventName, out var clipSource)) return;
        if (clipSource.source.isPlaying) CheckVoiceLimit();
        clipSource.PlayRandom();
    }

    private void CheckVoiceLimit()
    {
        var playingSources = new List<AudioSource>();
        foreach (var s in sources)
            if (s.source.isPlaying) playingSources.Add(s.source);

        if (playingSources.Count > maxVoices && playingSources.Count > 0)
            playingSources[0].Stop();
    }
}
```

### BallHitEvent.cs
**Путь:** `Assets/_Projects/Code/Futboloid.Core/Bus/Events/BallHitEvent.cs`

```csharp
public readonly struct BallHitEvent { }
```

### BallMotion.cs (изменения)
**Путь:** `Assets/_Projects/Code/Futboloid.Gameplay/Ball/BallMotion.cs`

Добавлена публикация `BallHitEvent` при ударе:
```csharp
private void ResolveHit(RaycastHit2D hit)
{
    Position = hit.point + hit.normal * (_settings.Radius + _settings.Skin);

    if (hit.collider.gameObject.layer == PhysicsLayers.KeeperId)
    {
        Direction = ClampMinAngle(Reflect(Direction, hit.normal));
        Speed = Mathf.Min(Speed + _settings.KeeperBoost, _settings.MaxSpeed);
        _bus.Publish(new BallReturnedToKeeperEvent());
        _bus.Publish(new BallHitEvent()); // <-- Добавлено
        return;
    }

    Direction = ClampMinAngle(Reflect(Direction, hit.normal));
    _bus.Publish(new BallHitEvent()); // <-- Добавлено
}
```

---

## Что нужно сделать в Unity Editor

1. **На сцене `Game.unity`:**
   - Найти объект `AudioManager`.
   - Создать дочерние объекты-динамики: `Speaker_BallHit`, `Speaker_Goal`, `Speaker_Start`, `Speaker_End`.
   - На каждый повесить `AudioClipSource`.
   - Заполнить `Clips`, `Mixer Group`, `Event Name`.

2. **Проверить `AudioManagerLifetimeScope`:**
   - Должен быть на сцене `Game.unity`.
   - `Auto Run` должен быть включён.

3. **Настроить AudioMixer:**
   - Создать группы: `SFX`, `Music`, `UI`.
   - Назначить их в `Mixer Group` динамиков.

---

## Скриншоты из чата

### Скриншот 1: Ошибка компиляции VContainer
**Дата:** 30.06.2026  
**Проблема:** `CS0246: The type or namespace name 'VContainer' could not be found`  
**Контекст:** Пытались использовать `[Inject]` в `Futboloid.Core`, но Core не должен зависеть от DI-фреймворка.

### Скриншот 2: NullReferenceException
**Дата:** 30.06.2026  
**Проблема:** `NullReferenceException: Object reference not set to an instance of an object` в `AudioManager.Update()`  
**Контекст:** `activeSources` мог быть null при первом вызове Update.

### Скриншот 3: Иерархия с ошибками
**Дата:** 30.06.2026  
**Контекст:** `AudioManagerLifetimeScope` создан, но ошибки компиляции ещё не исправлены.

---

## Архитектурные решения

1. **Почему не `[Inject]` в Core?** — Core должен быть чистым, без зависимостей от Unity и DI.
2. **Почему конструкторный инжект?** — VContainer автоматически вызовет конструктор с нужными параметрами.
3. **Почему система динамиков?** — Проще настраивать в Inspector, каждый звук — отдельный объект с своим массивом.
4. **Почему `public AudioSource source`?** — `AudioManager` должен обращаться к `source` для проверки `isPlaying` и управления.

---

## Следующие шаги

1. Настроить звуки в Unity Editor.
2. Протестировать воспроизведение при событиях.
3. Добавить звуки для `MenuClick`, `MenuHover`, `PauseToggle` (если нужно).
4. Настроить громкость в AudioMixer.
